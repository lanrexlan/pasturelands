# Pasturelands website

Next.js (App Router, TypeScript) + Tailwind CSS v4 site for Pasturelands Limited.
Build is in progress; see `PASTURELANDS_WEBSITE_HANDOVER.md` for the brief. Full
setup, Supabase and deploy notes will land here as those parts are built.

```bash
npm install
cp .env.example .env.local   # fill in what you have
npm run dev                  # http://localhost:3000
```
